# 🧪 VibePost - คู่มือการทดสอบระบบ (Manual Testing Guide)

ไฟล์นี้จัดทำขึ้นเพื่อใช้เป็น **Checklist** ในการตรวจสอบและทดสอบระบบทั้งหมดของ VibePost หลังจากมีการอัปเดตโครงสร้างและปรับเปลี่ยน Theme เป็น Light Corporate

---

## 1. การทดสอบหน้าแรก (Landing Page)
**เป้าหมาย:** ตรวจสอบการแสดงผลและข้อมูลสรุปที่หน้าแรก (ไม่มีการล็อกอิน)

- [ ] **1.1 โหลดหน้าเว็บ:** เข้าไปที่ `/` หน้าเว็บต้องโหลดขึ้นมาอย่างสมบูรณ์แบบ
- [ ] **1.2 การเปลี่ยนภาษา (Language Switcher):** ทดสอบกดธงชาติ (ไทย, อังกฤษ, จีน) ตรงมุมขวาบน ข้อความในหน้า Landing Page ต้องเปลี่ยนไปตามภาษาที่เลือก
- [ ] **1.3 สถิติภาพรวม (Stats):** ตรวจสอบว่ามีตัวเลขแสดง **โพสต์ทั้งหมด, แพลตฟอร์ม, และการส่งออกสำเร็จ** ถูกต้อง (ถ้าไม่มีข้อมูลในระบบ จะแสดงข้อมูล Demo Mode)
- [ ] **1.4 แกลเลอรีผลงาน (Auto-Post Gallery):** เลื่อนจอลงมาด้านล่างสุด ต้องเห็นช่องทาง Platform ต่างๆ และผลงานล่าสุดแสดงเป็นรูปแบบ Card ที่มีรูปภาพและเนื้อหาครบถ้วน และ**สามารถเลื่อนจอขึ้น-ลงได้อย่างอิสระ**

---

## 2. การทดสอบระบบเข้าสู่ระบบ (Authentication)
**เป้าหมาย:** ตรวจสอบการ Login และการจัดการ Session ของผู้ใช้

- [ ] **2.1 ไปยังหน้า Login:** คลิกปุ่ม `เข้าสู่ระบบ` (Login) จากมุมขวาบนของหน้า Landing Page
- [ ] **2.2 สมัครสมาชิก / เข้าสู่ระบบ:** เนื่องจากระบบใช้ `NextAuth` เชื่อมกับ Database (MongoDB/PostgreSQL) ให้ลองใส่ Email / Password เพื่อเข้าสู่ระบบ
- [ ] **2.3 เปลี่ยนเส้นทาง (Redirect):** เมื่อ Login สำเร็จ ระบบจะต้องพากลับมาที่หน้า `/dashboard` โดยอัตโนมัติ

---

## 3. การทดสอบระบบ Dashboard (ภาพรวมระบบ)
**เป้าหมาย:** ตรวจสอบ Theme สว่าง (Light Theme) และข้อมูลสรุป Workspace

- [ ] **3.1 เมนูด้านข้างและด้านบน (Sidebar & Header):** ตรวจสอบว่า Sidebar เป็นสีน้ำเงินกรมท่า (Navy Blue) และ Header เป็นสีขาว มีไอคอนแจ้งเตือน/โปรไฟล์มุมขวาบน
- [ ] **3.2 ข้อมูลสรุปภาพรวม:** ตรวจสอบจำนวน `Total Posts`, `Connected Accounts`, `Team Members`, `AI Configs` ว่าขึ้นเป็นตัวเลข (เริ่มต้นที่ 0 หรือตามฐานข้อมูลที่มี)
- [ ] **3.3 UI Check:** ตรวจสอบกรอบข้อความต่างๆ (3 ขั้นตอนการทำงาน) ว่าอ่านง่าย ตัวอักษรสีดำ/เทาเข้มบนพื้นหลังสีขาว ไม่กลืนกับพื้นหลังเหมือนตอนเป็น Dark Mode

---

## 4. การทดสอบสร้างแคมเปญโพสต์ด้วย AI (Multi-Post Wizard)
**เป้าหมาย:** ทดสอบฟังก์ชันการทำงานหลัก (Core Feature) ในการสร้างเนื้อหาพร้อมๆ กันหลายช่องทาง
*ตำแหน่ง: เมนู `New Campaign` หรือไปที่เมนูซ้ายมือ `AI Publisher`*

- [ ] **4.1 Step 1 (ใส่หัวข้อ/อัปโหลดภาพ):** พิมพ์หัวข้อคอนเทนต์ อัปโหลดรูปภาพ 1 รูป และเลือกช่องทาง (Platform) อย่างน้อย 1 ช่องทาง จากนั้นกดปุ่ม "Next: Generate AI Drafts" 
- [ ] **4.2 Step 2 (ระบบ AI สร้างแคปชัน):** ระบบควรแสดง "กำลังสร้างโพสต์..." และสักพักจะมีเนื้อหา Variation ต่างๆ ที่ AI คิดมาให้แสดงขึ้นมาบนหน้าจอ (การทดสอบนี้อาจต้องเช็คว่า AI Provider ถูกตั้งค่าไว้ใน Settings แล้วหรือไม่)
- [ ] **4.3 Step 3 (Review & Approval):** กดเลือก Variation ที่ต้องการ และกด "Next: Review & Schedule"
- [ ] **4.4 Step 4 (ตั้งเวลา หรือ โพสต์ทันที):** 
  - ลองเลือก `Post Now` และกด "Confirm & Publish"
  - หรือ ลองเลือก `Schedule for Later` เลือกวัน/เวลา และกด "Confirm & Schedule"
- [ ] **4.5 ตรวจสอบผลลัพธ์:** เมื่อเสร็จสิ้น ระบบควรเด้งข้อความสำเร็จและพาไปยังหน้า Auto Post History

---

## 5. การทดสอบหน้าประวัติการโพสต์ (Auto Post History)
**เป้าหมาย:** ตรวจสอบการเก็บประวัติหลังจากการโพสต์สำเร็จ (หรือตั้งเวลาไว้)
*ตำแหน่ง: เมนู `Post History`*

- [ ] **5.1 ความถูกต้องของประวัติ:** ตรวจสอบว่าโพสต์ที่สร้างจากขั้นตอนที่ 4 แสดงอยู่ในหน้านี้
- [ ] **5.2 ข้อมูลในตาราง:** ตรวจสอบความถูกต้องของคอลัมน์: **วันที่โพสต์, เนื้อหา, แพลตฟอร์ม, รูปภาพ, และ สถานะ**
- [ ] **5.3 ตัวเลขสถิติด้านบน:** สถิติ "โพสต์ทั้งหมด" และ "อัตราสำเร็จ" ด้านบนควรเพิ่มขึ้นตามข้อมูลจริง
- [ ] **5.4 Timeline โพสต์ล่าสุด:** แถบ Timeline แนวตั้งด้านล่างตาราง ต้องแสดง 5 โพสต์ล่าสุดถูกต้องเรียงตามเวลา

---

## 6. การทดสอบหน้าการตั้งค่าอื่นๆ (Settings & Integrations)

- [ ] **6.1 การตั้งค่า Social Connections:** (เมนู `Integrations`) ตรวจสอบหน้าตาของปุ่ม Connect Facebook, Instagram, Twitter, LinkedIn ว่าแสดงผลปกติ กดแล้วมี Popup เด้งขึ้นมาให้เชื่อมต่อ
- [ ] **6.2 การตั้งค่า AI (Settings):** (เมนู `Settings`) ตรวจสอบช่องกรอก API Key (เช่น OpenRouter, OpenAI) ลองพิมพ์และกดปุ่ม Save/Update ได้ปกติ
- [ ] **6.3 ทีมงาน (Workspaces):** (เมนู `Workspaces`) ตรวจสอบตารางแสดงรายชื่อ Team Members ของ Workspace ว่าแสดงผลข้อมูลชื่อ อีเมล และตำแหน่ง Role ถูกต้อง

---

## 7. การทดสอบความเสถียร (Build & Responsive)

- [ ] **7.1 Responsive Design:** ย่อหน้าจอเบราว์เซอร์ให้เป็นขนาดมือถือ (Mobile View) เพื่อทดสอบว่า Sidebar สามารถย่อ/ซ่อนได้ และกล่องข้อมูลต่างๆ เรียงต่อกันลงมา ไม่หลุดขอบ
- [ ] **7.2 Build Production:** รันคำสั่ง `npm run build` ในเครื่อง Server พบว่า **ผ่าน 100%** ไม่มี Error ขัดข้องที่ทำให้ระบบล่ม (ล่าสุดทดสอบผ่านแล้ว)
